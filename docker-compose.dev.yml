services:
  nginx:
    image: nginx:1.27
    container_name: registry-nginx
    restart: unless-stopped
    profiles:
      - dev
    ports:
      - "5080:5080"
      - "5081:5081"
    volumes:
      - ./nginx/conf.d/dev:/etc/nginx/conf.d:ro

  registry:
    image: registry:3.1.0
    container_name: registry-core
    restart: unless-stopped
    profiles:
      - dev
    environment:
      REGISTRY_HTTP_SECRET: ${REGISTRY_HTTP_SECRET:-change_me_registry_http_secret}
    volumes:
      - ./registry/config.dev.yml:/etc/distribution/config.yml.template:ro
      - ./registry/certs:/certs:ro
      - ./registry/data:/var/lib/registry
    depends_on:
      - redis

  postgres:
    image: postgres:16
    container_name: registry-postgres
    restart: unless-stopped
    profiles:
      - dev
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    container_name: registry-redis
    restart: unless-stopped
    profiles:
      - dev
    command:
      - redis-server
      - --requirepass
      - ${REDIS_PASSWORD}
      - --appendonly
      - "yes"
      - --maxmemory
      - 256mb
      - --maxmemory-policy
      - allkeys-lru
    volumes:
      - redis_dev_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build:
      context: ./api
    container_name: registry-api
    restart: unless-stopped
    profiles:
      - dev
    environment:
      API_PORT: ${API_PORT}
      DATABASE_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      REDIS_ADDR: redis:6379
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      REGISTRY_INTERNAL_URL: http://registry:5000
      REGISTRY_PUBLIC_URL: http://${DEV_REGISTRY_DOMAIN:-localhost:5080}
      REGISTRY_SERVICE: ${DEV_REGISTRY_DOMAIN:-localhost:5080}
      REGISTRY_TOKEN_ISSUER: ${REGISTRY_TOKEN_ISSUER}
      REGISTRY_TOKEN_REALM: http://${DEV_REGISTRY_DOMAIN:-localhost:5080}/api/registry/token
      REGISTRY_TOKEN_PRIVATE_KEY_PATH: /run/secrets/auth.key
      WEBHOOK_SECRET: ${WEBHOOK_SECRET}
      API_JWT_SECRET: ${API_JWT_SECRET}
      BOOTSTRAP_ADMIN_EMAIL: ${BOOTSTRAP_ADMIN_EMAIL}
      BOOTSTRAP_ADMIN_PASSWORD: ${BOOTSTRAP_ADMIN_PASSWORD}
      R2_ACCOUNT_ID: ${R2_ACCOUNT_ID}
      R2_ACCESS_KEY_ID: ${R2_ACCESS_KEY_ID}
      R2_SECRET_ACCESS_KEY: ${R2_SECRET_ACCESS_KEY}
      R2_BUCKET_NAME: ${R2_BUCKET_NAME}
      DEV_MODE: "true"
    volumes:
      - ./registry/certs/auth.key:/run/secrets/auth.key:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      registry:
        condition: service_started

  console:
    build:
      context: ./console
      args:
        - VITE_API_BASE_URL=http://${DEV_CONSOLE_DOMAIN:-localhost:5081}/api
    container_name: registry-console
    restart: unless-stopped
    profiles:
      - dev
    environment:
      VITE_API_BASE_URL: http://${DEV_CONSOLE_DOMAIN:-localhost:5081}/api
    depends_on:
      - api

volumes:
  postgres_dev_data:
  redis_dev_data:
